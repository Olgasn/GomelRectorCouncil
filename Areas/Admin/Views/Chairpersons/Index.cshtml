@model IEnumerable<GomelRectorCouncil.Models.Chairperson>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Председатели</h2>

<p>
    <a asp-action="Create">Добавить</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.StartDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.StopDate)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Rector.FullName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StopDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rector.FullName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ChairpersonId">Изменить</a> |
                <a asp-action="Details" asp-route-id="@item.ChairpersonId">Подробно</a> |
                <a asp-action="Delete" asp-route-id="@item.ChairpersonId">Удалить</a>
            </td>
        </tr>
}
    </tbody>
</table>
